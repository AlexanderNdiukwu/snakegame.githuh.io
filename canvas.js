const canvas = document.getElementById('myCanvas');
const ctx = canvas.getContext('2d');

// Drawing a rectangle
ctx.fillStyle = 'red';  // Set the fill color
ctx.fillRect(50, 50, 200, 150);  // Draw a rectangle (x, y, width, height)